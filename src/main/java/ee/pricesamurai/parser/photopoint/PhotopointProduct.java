package ee.pricesamurai.parser.photopoint;

import ee.pricesamurai.parser.Product;

public class PhotopointProduct extends Product {
    public PhotopointProduct(String name, float price, String url) {
        super(name, price, url);
    }
}
